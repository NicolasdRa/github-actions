name: Shell Commands

on: [push]

jobs:
  run-shel-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: multiline script
        run: | 
          node -v
          npm -v
          npm -v

